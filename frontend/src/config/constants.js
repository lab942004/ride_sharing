// How often the home page silently re-fetches the ride list in the
// background, so newly posted/removed rides show up without the user
// having to manually reload the page.
export const RIDES_POLL_INTERVAL_MS = 30_000 // 30s
